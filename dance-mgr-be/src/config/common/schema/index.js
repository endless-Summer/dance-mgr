const TopicMgr = {
  // 商品名
  name: String,
  // 价格 
  teacher: String,
  
  price: Number,
  // 剩余名额
  count: Number,
  // 生产日期
  producedDate: Number,
  // 分类
  classify: String,
};

module.exports = {
  TopicMgr,
};
